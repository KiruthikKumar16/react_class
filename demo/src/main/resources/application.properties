spring.application.name=demo
server.port=4090

# Database config — not used until Day 2.
# Uncomment these AND remove the @SpringBootApplication(exclude = {...}) in
# DemoApplication.java once you're ready to connect to a real database.
# spring.datasource.url=jdbc:postgresql://<supabase-host>:5432/postgres
# spring.datasource.username=postgres
# spring.datasource.password=<your-password>
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
